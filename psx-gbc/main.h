
#define VERSION '0.2.3'

//#define DEBUG  // Use this to use DEBUG Version of the Code. (Comment Out on Release)

#if defined(DEBUG)
#else
#	define RELEASE
#endif


typedef unsigned char  BYTE;
typedef unsigned short WORD;
typedef unsigned long DWORD;

